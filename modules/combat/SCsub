#!/usr/bin/env python
from misc.utility.scons_hints import *

Import("env")
Import("env_modules")

env_combat = env_modules.Clone()

# Combat substrate: Hitbox3D / Hurtbox3D (Area3D damage contract) + Projectile3D
# (manual-velocity Area3D projectile with swept collision). Standalone additive
# module (ADR 0008), standard module anatomy, auto-discovered by the build.
env_combat.add_source_files(env.modules_sources, "*.cpp")
